# Copy to .env for local runs.
#
# The current validated environment is still shared with clean-doc until this
# fork gets its own torch/MPS environment.
ENSEXAM_PYTHON=/Volumes/Tool/source/clean-doc/.venv-torch310-mps-stable/bin/python

