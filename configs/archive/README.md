# Archived Configs

These configs are retained for reproducing previous probes and rejected branches.

Do not use these as defaults. If one becomes useful again, copy it back to `configs/local/`, update `configs/README.md`, and add a short evaluation note to `docs/model-registry.md`.

Current archive includes smoke/probe, compare, older full-MPS, and preserve/leak sweep configs.
