# Current-Primary Broader Durable Exhaustion

## Decision

`KILL`. The current long-lived current-primary quality-improvement loop has no
remaining executable named failure bucket under the committed evidence. No
candidate achieved promotion-safe generalizing lift, and no model is promoted.
This terminal record closes the current ledger scope by durable exhaustion or
blockage, not by quality lift.

The terminal state is:

- cross-domain sidecar successors: exhausted by D3/D4/D5 terminal outcomes.
- external text-layout successors: exhausted after support passed but every
  registered edit-use route failed reachability or preserve separation before
  candidate admission.
- stroke-only source-candidate successors: exhausted unless exact historical
  selector-replay candidate PNGs are restored with hash custody.
- target-dark/overerase successors: exhausted after component-context,
  Delta-Trust, and safe-metric fallback routes failed before candidate
  admission.
- universal mechanism admission: blocked on the explicit product-owner entry
  decision required before the continuous three-expert reconstruction mixture is
  executable.

## Boundary

This is not a promotion event. `artifacts/current-primary` remains the product
default. Training, candidate inference, `inner_val15`, development gates,
SCUT115, holdout40, visual review, reserved blind, promotion, and current-primary
replacement remain closed.

Future re-entry requires a new materially named failure bucket with
leakage-safe train-only evidence, exact selector-replay PNG restoration with
hash custody, the product-owner unblock for universal mechanism admission, or a
new user-authorized quality program outside this exhausted ledger scope.

## Evidence

```text
broader exhaustion record = docs/current-primary-broader-durable-exhaustion-v1.json
successor inventory = docs/successor-selection-current-state-inventory-v1.json
quality loop ledger = docs/current-primary-quality-loop-ledger.json
failure bucket ledger = docs/current-primary-failure-ledger.md
program state after record = all_exhausted
promotion outcome = no_candidate_promoted
```

Intent: Close the current evidence-gated quality loop only after every currently named failure bucket is terminal, blocked, or out of scope.
Constraint: The objective permits completion by reserved-blind success or by durable exhaustion/blockage; this path is the latter and must not imply model improvement.
Rejected: Promote the best prior candidate | source-edge reached SCUT115 but failed page-level no-regression, so reserved blind and promotion remain closed.
Rejected: Keep successor selection active indefinitely | the current inventory contains no executable leakage-safe bucket, only blocked or exhausted routes.
Rejected: Treat future unknown buckets as already tested | re-entry requires a new named bucket and evidence rather than reopening this terminal scope.
Confidence: high
Scope-risk: moderate
Directive: Do not replace `artifacts/current-primary` or reopen closed buckets from this record. Start a new quality program only with materially new evidence or explicit unblock authority.
Tested: `pytest tests/test_report_current_primary_quality_loop_status.py` passed 17/17; `report_current_primary_program_status.py` passed with program_state=all_exhausted, active_bucket=None, 10 buckets, and zero failures; `report_current_primary_quality_loop_status.py` reported all_failure_buckets_exhausted_or_blocked with lifecycle_state=all_exhausted, no pending prerequisites, blockers=[], candidate_admission_ready=false, promotion_eligible=false, reserved_blind_authorized=false, and successor_readiness=not_blocked_by_evidence_audit.
Not-tested: New candidate training, candidate inference, validation gates, SCUT115, holdout40, visual review, reserved blind, promotion, or current-primary replacement.
